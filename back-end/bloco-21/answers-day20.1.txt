SELECT MAX(SALARY) FROM hr.employees

SELECT MAX(SALARY) - MIN(SALARY) FROM hr.employees

SELECT JOB_ID, AVG(SALARY) AS Average FROM hr.employees
GROUP BY JOB_ID
ORDER BY Average

SELECT SUM(SALARY) FROM hr.employees;

SELECT
	ROUND(MAX(SALARY) ,2) AS Max,
	ROUND(MIN(SALARY) ,2) AS Min,
	ROUND(AVG(SALARY) ,2) AS Average,
	ROUND(SUM(SALARY) ,2) As Sum
FROM
	hr.employees

SELECT COUNT(*) FROM hr.employees
WHERE JOB_ID = 'IT_PROG'

SELECT JOB_ID, SUM(SALARY) FROM hr.employees
GROUP BY JOB_ID

SELECT JOB_ID, SUM(SALARY) FROM hr.employees
WHERE JOB_ID = 'IT_PROG'
GROUP BY JOB_ID

SELECT JOB_ID, AVG(SALARY) AS AVERAGE FROM hr.employees
WHERE JOB_ID <> 'IT_PROG'
GROUP BY JOB_ID
ORDER BY AVERAGE DESC

SELECT DEPARTMENT_ID, AVG(SALARY) AS AVERAGE, COUNT(*) AS `NUMBER` FROM hr.employees
GROUP BY DEPARTMENT_ID


SET SQL_SAFE_UPDATES = 0;
UPDATE hr.employees
SET PHONE_NUMBER = CONCAT('777', SUBSTRING(PHONE_NUMBER, 4))
WHERE LEFT(PHONE_NUMBER, 3) = '515';
SELECT * FROM hr.employees LIMIT 10;

SELECT * FROM hr.employees
WHERE LENGTH(first_name) >= 8

SELECT EMPLOYEE_ID, FIRST_NAME, YEAR(DATE(HIRE_DATE)) FROM hr.employees

SELECT EMPLOYEE_ID, FIRST_NAME, DAY(DATE(HIRE_DATE)) FROM hr.employees

SELECT EMPLOYEE_ID, FIRST_NAME, MONTH(DATE(HIRE_DATE)) FROM hr.employees

SELECT LCASE(FIRST_NAME) FROM hr.employees

SELECT LAST_NAME FROM hr.employees
WHERE LEFT(HIRE_DATE ,7) = '1987-07'

SELECT FIRST_NAME, LAST_NAME, DATEDIFF(DATE(NOW()), HIRE_DATE) AS 'TIME_WORK' FROM hr.employees LIMIT 10;

